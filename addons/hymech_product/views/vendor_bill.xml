    <odoo>
    <data>
        <record model="ir.ui.view" id="view_custom_account_move_form">
            <field name="name">custom.account.move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <field name="invoice_date" position="after">
              
                <field name="purchase_by" invisible="move_type != 'in_invoice'"/>

                </field>
                <!-- <xpath expr="//field[@name='amount_by_group']" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath> -->
                <!-- <field name="amount_untaxed" position="after">
                    <field name="gst_tax_total" />
                </field> -->
            </field>
        </record>
    </data>
</odoo>